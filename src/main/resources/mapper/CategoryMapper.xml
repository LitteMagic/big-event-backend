<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itheima.bigeventbackend.mapper.CategoryMapper">

    <insert id="insert" parameterType="com.itheima.bigeventbackend.pojo.Category">
        insert into category
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="categoryName != null">category_name,</if>
            <if test="categoryAlias != null">category_alias,</if>
            <if test="createUser != null">create_user,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="categoryName != null">#{categoryName},</if>
            <if test="categoryAlias != null">#{categoryAlias},</if>
            <if test="createUser != null">#{createUser},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="update" parameterType="com.itheima.bigeventbackend.pojo.Category">
        update category set
        <set>
            <if test="categoryName != null"> category_name = #{categoryName} ,</if>
            <if test="categoryAlias != null">category_alias = #{categoryAlias} ,</if>
            <if test="updateTime != null">update_time = #{updateTime} ,</if>
        </set>
        where id = #{id}
    </update>

</mapper>